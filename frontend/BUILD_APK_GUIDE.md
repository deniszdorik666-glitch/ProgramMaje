# 📱 Подробная инструкция по сборке APK для Majestic RP Launcher

## 📋 Содержание
1. [Подготовка компьютера](#1-подготовка-компьютера)
2. [Установка Node.js](#2-установка-nodejs)
3. [Установка Java JDK](#3-установка-java-jdk)
4. [Установка Android Studio](#4-установка-android-studio)
5. [Настройка переменных среды](#5-настройка-переменных-среды)
6. [Подготовка проекта](#6-подготовка-проекта)
7. [Сборка APK](#7-сборка-apk)
8. [Установка на телефон](#8-установка-на-телефон)
9. [Решение проблем](#9-решение-проблем)

---

## 1. Подготовка компьютера

### Системные требования:
- **ОС:** Windows 10/11, macOS 10.14+, или Linux
- **RAM:** минимум 8 ГБ (рекомендуется 16 ГБ)
- **Свободное место:** минимум 20 ГБ
- **Интернет:** стабильное подключение

---

## 2. Установка Node.js

### Windows:
1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (зелёная кнопка)
3. Запустите установщик
4. Нажимайте "Next" на всех шагах
5. **Важно:** Поставьте галочку "Automatically install necessary tools"
6. Дождитесь завершения установки

### Проверка установки:
Откройте **Командную строку** (Win + R → cmd) и введите:
```bash
node --version
```
Должно показать версию, например: `v20.10.0`

```bash
npm --version
```
Должно показать версию, например: `10.2.3`

---

## 3. Установка Java JDK

### Windows:
1. Перейдите на https://adoptium.net/
2. Выберите **Temurin 17 (LTS)**
3. Скачайте **.msi** установщик для Windows
4. Запустите установщик
5. **ВАЖНО:** На шаге "Custom Setup" включите:
   - ✅ "Set JAVA_HOME variable"
   - ✅ "JavaSoft (Oracle) registry keys"
6. Завершите установку

### Проверка установки:
Откройте **НОВУЮ** командную строку и введите:
```bash
java --version
```
Должно показать что-то вроде:
```
openjdk 17.0.9 2023-10-17
OpenJDK Runtime Environment Temurin-17.0.9+9 (build 17.0.9+9)
```

---

## 4. Установка Android Studio

### Шаг 4.1: Скачивание
1. Перейдите на https://developer.android.com/studio
2. Нажмите "Download Android Studio"
3. Примите условия и скачайте установщик

### Шаг 4.2: Установка
1. Запустите скачанный файл
2. Нажмите "Next"
3. Выберите компоненты (оставьте все по умолчанию)
4. Выберите папку установки (оставьте по умолчанию)
5. Нажмите "Install"
6. Дождитесь завершения (может занять 10-20 минут)

### Шаг 4.3: Первый запуск и настройка SDK
1. Запустите Android Studio
2. Выберите "Do not import settings" → OK
3. Нажмите "Next" на экране приветствия
4. Выберите "Standard" установку → Next
5. Выберите тему (любую) → Next
6. Дождитесь скачивания компонентов (может занять 10-30 минут)
7. Нажмите "Finish"

### Шаг 4.4: Установка дополнительных SDK компонентов
1. В Android Studio нажмите "More Actions" → "SDK Manager"
   (или File → Settings → Languages & Frameworks → Android SDK)

2. Во вкладке **"SDK Platforms"**:
   - ✅ Android 14.0 ("UpsideDownCake") - API Level 34
   - ✅ Android 13.0 ("Tiramisu") - API Level 33
   
3. Во вкладке **"SDK Tools"**:
   - ✅ Android SDK Build-Tools 34
   - ✅ Android SDK Command-line Tools (latest)
   - ✅ Android Emulator
   - ✅ Android SDK Platform-Tools
   - ✅ NDK (Side by side) - последняя версия

4. Нажмите "Apply" и дождитесь установки

### Шаг 4.5: Запомните путь к SDK
В настройках SDK вверху будет написано:
```
Android SDK Location: C:\Users\ВашеИмя\AppData\Local\Android\Sdk
```
**Скопируйте этот путь** - он понадобится далее!

---

## 5. Настройка переменных среды

### Windows:

#### Шаг 5.1: Открыть настройки переменных среды
1. Нажмите Win + R
2. Введите `sysdm.cpl` и нажмите Enter
3. Перейдите на вкладку "Дополнительно"
4. Нажмите "Переменные среды..."

#### Шаг 5.2: Добавить ANDROID_HOME
1. В разделе "Переменные среды пользователя" нажмите "Создать"
2. Имя переменной: `ANDROID_HOME`
3. Значение переменной: `C:\Users\ВашеИмя\AppData\Local\Android\Sdk`
   (путь, который вы скопировали из Android Studio)
4. Нажмите OK

#### Шаг 5.3: Добавить JAVA_HOME (если не добавилось автоматически)
1. Нажмите "Создать"
2. Имя переменной: `JAVA_HOME`
3. Значение переменной: `C:\Program Files\Eclipse Adoptium\jdk-17.0.9.9-hotspot`
   (путь может отличаться, проверьте в папке Program Files)
4. Нажмите OK

#### Шаг 5.4: Добавить пути в PATH
1. Найдите переменную `Path` в списке и нажмите "Изменить"
2. Нажмите "Создать" и добавьте следующие пути (каждый отдельно):
   ```
   %ANDROID_HOME%\platform-tools
   %ANDROID_HOME%\tools
   %ANDROID_HOME%\tools\bin
   %ANDROID_HOME%\cmdline-tools\latest\bin
   ```
3. Нажмите OK на всех окнах

#### Шаг 5.5: Проверка
**ЗАКРОЙТЕ ВСЕ** окна командной строки и откройте новое. Введите:
```bash
adb --version
```
Должно показать версию ADB.

---

## 6. Подготовка проекта

### Шаг 6.1: Скачайте проект
1. В интерфейсе Emergent нажмите кнопку **"Download"**
2. Скачается ZIP архив
3. Распакуйте архив в удобную папку, например:
   `C:\Projects\MajesticRP`

### Шаг 6.2: Откройте терминал в папке frontend
1. Откройте папку `frontend` внутри распакованного проекта
2. Зажмите Shift + ПКМ (правая кнопка мыши) на пустом месте
3. Выберите "Открыть окно PowerShell здесь" или "Открыть в Терминале"

Или через командную строку:
```bash
cd C:\Projects\MajesticRP\frontend
```

### Шаг 6.3: Установите зависимости
```bash
npm install
```
Дождитесь завершения (может занять 2-5 минут)

### Шаг 6.4: Удалите лишние переменные из .env
Откройте файл `frontend/.env` в текстовом редакторе и **удалите или закомментируйте** строки:
```
# EXPO_TUNNEL_SUBDOMAIN=...
# EXPO_PACKAGER_HOSTNAME=...
# EXPO_PUBLIC_BACKEND_URL=...
```
Или просто удалите файл .env (он не нужен для APK)

---

## 7. Сборка APK

### Шаг 7.1: Создайте нативный Android проект
```bash
npx expo prebuild --platform android --clean
```
Это создаст папку `android` с нативным кодом.

### Шаг 7.2: Перейдите в папку android
```bash
cd android
```

### Шаг 7.3: Соберите APK

#### Для Debug версии (быстрее, для тестирования):
```bash
# Windows
.\gradlew.bat assembleDebug

# Mac/Linux
./gradlew assembleDebug
```

#### Для Release версии (оптимизированная, для распространения):
```bash
# Windows
.\gradlew.bat assembleRelease

# Mac/Linux  
./gradlew assembleRelease
```

⏱️ **Первая сборка может занять 10-30 минут!** Последующие сборки будут быстрее.

### Шаг 7.4: Найдите готовый APK

**Debug APK:**
```
android/app/build/outputs/apk/debug/app-debug.apk
```

**Release APK:**
```
android/app/build/outputs/apk/release/app-release.apk
```

---

## 8. Установка на телефон

### Способ 1: Через USB кабель
1. На телефоне включите **"Отладка по USB"**:
   - Настройки → О телефоне → 7 раз нажать на "Номер сборки"
   - Настройки → Для разработчиков → Включить "Отладка по USB"
2. Подключите телефон к компьютеру
3. В командной строке выполните:
   ```bash
   adb install путь\к\app-debug.apk
   ```

### Способ 2: Перенос файла
1. Скопируйте APK файл на телефон (через USB, облако, мессенджер)
2. На телефоне разрешите установку из неизвестных источников:
   - Настройки → Безопасность → Разрешить установку из неизвестных источников
3. Откройте APK файл на телефоне и нажмите "Установить"

---

## 9. Решение проблем

### Проблема: "JAVA_HOME is not set"
**Решение:** Убедитесь, что переменная JAVA_HOME настроена правильно и указывает на папку JDK (не JRE).

### Проблема: "SDK location not found"
**Решение:** Создайте файл `local.properties` в папке `android` с содержимым:
```
sdk.dir=C:\\Users\\ВашеИмя\\AppData\\Local\\Android\\Sdk
```
(Обратите внимание на двойные обратные слэши!)

### Проблема: "Could not find tools.jar"
**Решение:** Установите JDK, а не JRE. Убедитесь, что JAVA_HOME указывает на JDK.

### Проблема: Gradle скачивает долго
**Решение:** Это нормально при первой сборке. Просто дождитесь завершения.

### Проблема: "Execution failed for task ':app:processDebugResources'"
**Решение:** 
```bash
cd android
.\gradlew.bat clean
.\gradlew.bat assembleDebug
```

### Проблема: Не хватает памяти
**Решение:** Создайте файл `gradle.properties` в папке `android` с содержимым:
```
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m
```

---

## 🎉 Готово!

После успешной сборки у вас будет APK файл, который можно установить на любой Android телефон!

### Размер APK
- Debug: ~50-80 МБ
- Release: ~30-50 МБ

### Минимальная версия Android
- Android 6.0 (API 23) и выше

---

## 📞 Нужна помощь?

Если возникли проблемы, опишите:
1. На каком шаге возникла ошибка
2. Полный текст ошибки
3. Вашу операционную систему
